# Evidence interpretation

`artifacts/real-update-acceptance/evidence.json` is the primary machine-readable record. The final review must confirm that installer hashes, executable hashes, journal states, preservation hashes, browser registry snapshots, exit codes, and `testedCommitSha` all correspond to the same successful workflow run.
